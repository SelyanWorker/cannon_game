add_library(game_objects_
        INTERFACE
        )
target_include_directories(game_objects_
        INTERFACE
        ${CMAKE_CURRENT_SOURCE_DIR}/include
        )
target_link_libraries(game_objects_
        INTERFACE
        glm
        sprite
        )
